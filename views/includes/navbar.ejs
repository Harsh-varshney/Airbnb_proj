<style>
    .btn-search{
        background-color: #fe424d;
        color: white;
        border-radius: 2rem;
        padding: 0.7rem 1rem;
    }
    .btn-search:hover{
        background-color: #fe424d;
        color: white;
    }
    .btn-search i{
        display: inline;
        margin-right: 0.5rem;
    }

    .search-input{
        border-radius: 2rem;
        padding: 0 3.5rem;
        font-size: 0.8rem;
        font-family: 'Plus Jakarta Sans', sans-serif !important;
    }

    .search-input:focus {
        outline: none !important;
        box-shadow: 0 0 0 2px rgba(255, 0, 0, 0.25) !important; /* Red shadow */
        border-color: red !important; /* Red border */
    }
</style>

<nav class="navbar navbar-expand-sm bg-body-light border-bottom border-dark sticky-top">
    <div class="container-fluid">
        <!-- <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a> -->
         <a class="navbar-brand" href="/listings"><img src="/img/airbnb_logo.png"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/listings">Explore</a>
            </div>
            <div class="navbar-nav ms-auto">
                <form action="/listings/search" class="d-flex" role="search" method="get">
                    <input class="form-control me-2 search-input" name="query"  type="search" placeholder="Search Destinations" />
                    <button class="btn btn-search" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
                </form>
            </div>
            <div class="navbar-nav ms-auto"> <!-- currUser stored in local-->
                <a class="nav-link" href="/listings/new">Airbnb your home</a>
                <% if (!currUser) { %> 
                    <a class="nav-link" href="/signup"><b>Sign up</b></a>
                    <a class="nav-link" href="/login"> <b>Log in</b></a>
                <% } %>
                <% if (currUser) { %>
                    <a class="nav-link" href="/logout"> <b>Log out</b></a>
                <% } %>
            </div>
        </div>
    </div>
</nav>
